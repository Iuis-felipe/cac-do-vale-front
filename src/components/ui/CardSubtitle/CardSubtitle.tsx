import { TypographyProps } from '@mui/material';
import { CardSubtitleStyled } from './CardSubtitle.styled';

export function CardSubtitle(props: TypographyProps) {
  return <CardSubtitleStyled {...props} />;
}
